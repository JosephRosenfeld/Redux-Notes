/*These don't have to be action types but they typically are. We house all action
types in one file so we only have to make edits in one place*/

export const DEPOSIT = "DEPOSIT";
export const WITHDRAW = "WITHDRAW";
